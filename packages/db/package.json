{
  "name": "@ai-workspace/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema.ts",
    "./client": "./src/client.ts"
  },
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "db:generate": "echo 'BLOCKED (#449): drizzle meta snapshots are intentionally frozen at 0023 while migrations run to 0035+. Migrations are handwritten SQL + a _journal.json entry \u2014 see packages/db/README.md. Running drizzle-kit generate/check against the stale meta emits WRONG output.' && exit 1",
    "db:migrate": "tsx src/migrate.ts",
    "db:studio": "drizzle-kit studio",
    "db:check": "echo 'BLOCKED (#449): drizzle meta snapshots are intentionally frozen at 0023 while migrations run to 0035+. Migrations are handwritten SQL + a _journal.json entry \u2014 see packages/db/README.md. Running drizzle-kit generate/check against the stale meta emits WRONG output.' && exit 1"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "@types/node": "^22.9.0",
    "drizzle-kit": "^0.28.1",
    "eslint": "^9.15.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.16.0"
  }
}
