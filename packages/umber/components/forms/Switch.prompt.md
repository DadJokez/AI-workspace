**Switch** — instant on/off toggle for a setting (no Save step). Controlled or uncontrolled.

```jsx
<Switch label="Public sharing" defaultChecked />
<Switch size="sm" checked={dark} onChange={e => setDark(e.target.checked)} />
```
