**Textarea** — multi-line text entry, matched to Input's styling.

```jsx
<Textarea label="Description" rows={5} hint="Markdown supported." />
```

Vertically resizable. Supports `label`, `hint`, `error`, `rows`, `disabled`.
