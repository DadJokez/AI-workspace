**Tooltip** — a short hover/focus label. Wrap any trigger; keep content to a few words.

```jsx
<Tooltip content="Copy endpoint" side="top">
  <IconButton aria-label="Copy"><Icon name="copy" /></IconButton>
</Tooltip>
```

`side`: `top | bottom | left | right`.
