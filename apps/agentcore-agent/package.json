{
  "name": "@ai-workspace/agentcore-agent",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "AI Hub agent loop hosted on Amazon Bedrock AgentCore Runtime (specs/003 spike). Speaks the AgentCore HTTP service contract: POST /invocations (SSE out), GET /ping.",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@ai-workspace/agent": "workspace:*",
    "tsx": "^4.19.2"
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "@types/node": "^22.9.0",
    "eslint": "^9.15.0",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.16.0",
    "vitest": "^2.1.9"
  }
}
