# Week-1 hardcoded user for the auth shim (packages/auth).
# Replaced by PingOne OIDC session in week 2 — these vars become unused then.
#
# Copy this file to `.env.local` and fill in your own values for local dev.
# In Fargate, set these on the task definition's environment.
HARDCODED_USER_ID=dev-user-001
HARDCODED_USER_EMAIL=dev@example.com
HARDCODED_USER_NAME=Local Dev
